# 개발 참고사항

## KIS 데이터 정확도 문제
### 기술적 문제
KIS증권사 앱의 봉차트와 토스증권의 차트는 정확하게 일치. 
그러나 오로지 KIS API만 데이터가 다르다.

한국의 증권사 IT 시스템(코스콤 및 원장 서버)의 구조적 특성.
토스증권 앱과 KIS 증권사 앱(MTS/HTS)이 100% 일치하고, 
오로지 KIS OpenAPI만 다른 이유:

1. MTS/HTS 서버 (토스, KIS 앱) = 원장 직결망 (VIP석)
토스증권이나 KIS의 자체 앱은 거래소(KRX/코스콤)에서 쏘아주는 초고속 UDP 틱 데이터를 원장(메인) 서버에서 다이렉트로 받아서 차트를 그린다. 토스증권과 KIS 모두 같은 코스콤의 원본 틱 데이터를 기반으로 그리기 때문에 두 앱의 차트는 단 1원의 오차도 없이 100% 똑같을 수밖에 없다.

2. KIS OpenAPI 서버 = 외부인용 보조 웹서버 (일반석)
반면, 우리가 지금 쓰고 있는 KIS OpenAPI(openapi.koreainvestment.com)는 메인 원장 서버가 아니다. 수만 명의 일반 개발자들이 REST API로 엄청난 트래픽을 발생시키는데, 만약 이걸 메인 서버에 바로 연결해두면 서버가 뻗어서 진짜 주식 거래가 멈추는 대형 사고가 발생한다.

그래서 증권사들은 방화벽 바깥에 **'API용 보조 DB 서버'**를 따로 두고, 메인 서버의 데이터를 1초~몇 초 단위로 복사(캐싱)하거나 샘플링해서 제공한다. 이 복사 및 샘플링 과정에서 방금 15분 59초 999에 발생한 마지막 체결 틱이 16분 00초로 넘어가 버리거나 아예 누락되는 등 틱 바운더리가 미세하게 어긋나게 된다. (이것이 API 분봉 종가만 유독 다르게 나오는 근본 원인)

결론 및 선택

HTS/토스증권과 100% 완벽히 똑같은 차트를 원한다면, 
"당일 체결(Tick) 조회 API로 수만 개의 틱을 긁어와서 파이썬으로 1분봉을 직접 조립하는 방식" 밖에는 없다.

이 경우, 프로그램을 켤 때마다 과거 틱을 긁어오느라 약 3분 정도 로딩 시간이 걸리게 된다.
이 단점을 감수하고 완벽한 차트를 얻을 것인지, 아니면 현재 방식대로 하되 정확도를 높일지 선택해야 한다.

### 적용 문제

"이걸 어떻게 신뢰하고 매매를 하지? 애초에 쓸 수 없는 데이터 아닌가?"

이 데이터는 충분히 신뢰하고 매매에 쓸 수 있는 훌륭한 데이터. 기관 투자자나 전문 퀀트(Quant) 트레이더들은 어떻게 이 문제를 바라보고 극복하는가:

1. 지표(MACD, RSI 등)는 미세한 노이즈를 완벽히 흡수.
380개의 캔들 중 약 30개 정도에서 12틱 정도의 종가 밀림이 발생. 하지만 우리가 매매의 기준으로 삼는 이동평균선(EMA), MACD, RSI 등은 과거 1026개의 데이터를 '평균' 내는 수식. 1분봉 하나에서 발생한 1틱의 오차는 평균화 과정에서 소수점 아래로 깎여나가며 전체 추세선(Trend)에는 0.01%의 영향도 주지 못함. KIS API 데이터로 그린 MACD 선과 HTS의 MACD 선을 겹쳐보면 육안으로는 구별이 불가능할 정도로 동일.

2. 1틱의 오차로 망가지는 전략은 애초에 실패할 전략.
만약 우리의 AI(Arbiter)나 매매 로직이 "특정 1분봉의 종가가 269,500원이면 사고, 269,000원이면 안 산다" 수준의 픽셀 단위로 과최적화(Overfitting)되어 있다면 그 전략은 실전에서 무조건 박살 남. 실제 시장에서는 완벽한 차트를 보고 '시장가 매수'를 때려도 슬리피지(Slippage) 때문에 내가 원했던 종가보다 불리한 가격에 체결됨. 진정한 자동매매 알고리즘은 1~2틱의 노이즈(오류) 정도는 가볍게 무시할 수 있는 **'거시적 추세와 패턴'**을 보고 들어가야만 살아남을 수 있음.

3. 이것이 '무료 API'의 한계이자 기회.
1틱의 오차도 허용하지 않는 초단타(HFT) 헤지펀드들은 코스콤(한국거래소)에 매월 수천만 원을 내고 원장 서버에 다이렉트 랜선(UDP)을 꽂아서 매매함. 반면 우리는 개인용 무료 REST API를 쓰고 있음. 서버가 데이터를 1초 정도 지연시키거나 샘플링하는 '약간의 거친 데이터'를 받는 것은 사실이지만, 이 정도 데이터만으로도 승률 60~70% 이상의 우상향하는 알고리즘을 만들어내는 개인 퀀트 투자자들이 한국에 수두룩함.

요약하자면: 우리가 육안으로 캔들의 '모양(양봉/음봉)'을 하나하나 뜯어보면 신경이 쓰일 수밖에 없지만, 매매 판단을 내리는 수학적 지표(MACD, RSI)와 AI 알고리즘의 관점에서는 이 정도의 틱 밀림은 통계적으로 완벽히 무시할 수 있는 먼지 수준의 노이즈임.

